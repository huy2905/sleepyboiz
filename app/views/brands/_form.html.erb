<%= form_with(model: brand) do |form| %>
<% if brand.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% brand.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :brandID, class: label_class %>
          <%= form.number_field :brandID, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :brandName, class: label_class %>
          <%= form.text_field :brandName, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :logo, class: label_class %>
          <%= form.file_field :logo, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :description, class: label_class %>
          <%= form.text_area :description, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if brand.persisted? %>
<%= link_to "Cancel", brand, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", brands_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
